{
  "buildCommand": "pnpm turbo run build:vercel --filter=web",
  "outputDirectory": "apps/web/out",
  "installCommand": "pnpm install",
  "framework": null,
  "ignoreCommand": "git diff HEAD^ HEAD --quiet ."
}
